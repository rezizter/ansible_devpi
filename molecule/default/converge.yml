---
- name: Converge
  hosts: all
  tasks:
    - name: Install packages
      yum:
        name:
          - python3
          - python3-setuptools
          - python3-pip
        state: present
    - name: "Include role"
      ansible.builtin.include_role:
        name: "rezizter.ansible_devpi"
